# ClinicalTrialAI — Phase 1 Implementation Plan

## Goal
Deliver a Phase‑1 vertical slice that ingests synthetic FHIR, de‑identifies, maps to OMOP, loads a small Neo4j graph, runs a multi‑agent simulation (cohort→protocol→adversary→judge), exports an IRB‑ready report, and logs all actions — without touching real PHI.

## Scope
- Synthetic Synthea input only
- Core data contracts (FHIR→OMOP; AgentMessage; AuditEvent)
- LangGraph orchestrator + 4 agent stubs
- Neo4j schema + pgvector retriever
- FastAPI route + CLI
- Audit ledger abstraction (QLDB later; file‑backed now)
- Zero‑PHI LLM boundary with tests

## Deliverables
- Repo scaffold per docs/FILE_STRUCTURE.md
- Ingestion DAG: FHIR→de‑id→OMOP (selected OMOP tables)
- Graph loaders + cohort Cypher queries
- Agents with typed messages and prompts
- LLM provider adapters (Azure OpenAI + local vLLM stub)
- API route `/simulate` and CLI `scripts/run_simulation.py`
- Audit ledger stub and security tests
- Helm chart and Terraform skeletons (dev focus)

## Milestones
1) Scaffold + ingestion+graph+cohort agent (2 wks)
2) Protocol/adversary/judge + orchestrator + API (2 wks)
3) Report export + observability + hardening (2 wks)

## Definition of Done (Phase 1)
- End‑to‑end run completes on synthetic data in <30 min
- No PHI appears in any LLM prompts/responses (tests enforced)
- Jaeger trace includes `simulation_id` across components
- IRB‑ready export bundle generated by CLI
- Helm installs dev slice on a cluster

## Notes
- Decisions align with docs/PLAN.md decision log
- Security boundaries per docs/DESIGN.md Zones 1/2
